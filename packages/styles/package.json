{
  "name": "@radius/styles",
  "version": "0.0.3",
  "description": "Just the tokens for the design system",
  "main": "index.css",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:prefix": "postcss *.css --use autoprefixer -d dist/",
    "build": "npm run build:prefix"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.11",
    "components": "^0.1.0",
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0"
  }
}
