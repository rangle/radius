import { useState } from 'react';
import {
  Meta,
  Story,
  Preview,
} from '@storybook/addon-docs/blocks';
import { Box, Label } from '../';
import { TextArea } from './text-area';

<Meta
  title="Recipes/TextArea"
  component={TextArea}
  parameters={{
    a11y: {
      config: {
        rules: [{ id: 'label', enabled: false }],
      },
    },
  }}
/>

# TextArea Recipes

<Preview>
  <Story name="default text area">
    <Box>
      <Label adornment="(optional)" mb={4}>Write a review</Label>
      <TextArea label="Writing a review" maxCharacters={100} />
    </Box>
  </Story>
</Preview>