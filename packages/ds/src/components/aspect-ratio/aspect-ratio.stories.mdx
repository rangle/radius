import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { AspectRatio } from './aspect-ratio';
import { Heading } from '../heading';

<Meta title="Components/AspectRatio" component={AspectRatio} />

# AspectRatio

Extension of the `Box` component that maintains content within a desired ratio.

<Preview withToolbar>
  <Story name="AspectRatio">
    <AspectRatio
      bg="primary"
      p={3}
      display="flex"
      alignItems="center"
      justifyContent="center"
    >
      <Heading color="text">Aspect Ratio</Heading>
    </AspectRatio>
  </Story>
</Preview>

## Component Props

<Props of={AspectRatio} />
